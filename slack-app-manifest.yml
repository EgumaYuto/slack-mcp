# slack-mcp App Manifest
# https://api.slack.com/apps → Create New App → From a manifest でこの内容を貼り付けると、
# 必要な User Token Scopes が一括で設定されます。
display_information:
  name: slack-mcp
  description: Slack read + post access for the slack-mcp MCP server.
oauth_config:
  scopes:
    user:
      - channels:read
      - channels:history
      - groups:read
      - groups:history
      - im:read
      - im:history
      - mpim:read
      - mpim:history
      - search:read
      - users:read
      # 投稿・リアクション・編集・削除用
      - chat:write
      - reactions:write
settings:
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
