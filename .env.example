# Slack user token (xoxp-...). Required.
# Create a Slack app, add the user-token scopes listed in the README,
# install it to your workspace, and copy the "User OAuth Token".
SLACK_TOKEN=xoxp-your-user-token-here
